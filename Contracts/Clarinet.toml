[project]
name = "StackFund-DAO"
description = "StackFund DAO contracts"
authors = []
telemetry = false
cache_dir = ".cache"

[repl]
clarity_version = 2
epoch = 2.1

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false

[contracts.proposal-registry-minimal]
path = "contracts/proposal-registry-minimal.clar"

[contracts.voting-minimal]
path = "contracts/voting-minimal.clar"

[contracts.treasury-minimal]
path = "contracts/treasury-minimal.clar"